name: Build & Release

# 触发条件：推送 tag（如 v1.0.24）或手动触发
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (如 1.0.24)'
        required: true
        type: string

# 授予 GITHUB_TOKEN 写入 Release 的权限
permissions:
  contents: write

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Generate signing keystore
        run: |
          keytool -genkey -v \
            -keystore app/talkify-ci.jks \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -alias talkify \
            -storepass talkify123 \
            -keypass talkify123 \
            -dname "CN=TalkifyCI, OU=Talkify, O=TalkifyTTS, L=CI, ST=CI, C=CN"

      - name: Build Release APK
        run: ./gradlew assembleRelease
        env:
          TALKIFY_KEYSTORE_PATH: app/talkify-ci.jks
          TALKIFY_STORE_PASSWORD: talkify123
          TALKIFY_KEY_ALIAS: talkify
          TALKIFY_KEY_PASSWORD: talkify123

      - name: Find APK
        id: find_apk
        run: |
          APK_PATH=$(find app/build/outputs/apk/release -name "*.apk" ! -name "*-unsigned*" | head -1)
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "apk_name=$(basename "$APK_PATH")" >> $GITHUB_OUTPUT

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: Talkify-Release-APK
          path: ${{ steps.find_apk.outputs.apk_path }}

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          files: ${{ steps.find_apk.outputs.apk_path }}
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
